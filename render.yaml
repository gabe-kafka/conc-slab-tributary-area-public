services:
  - type: web
    name: conc-slab-tributary-area-public
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.8"
      - key: TRIBUTARY_APP_DATA
        value: /tmp/tributary-data
      - key: CORS_ORIGIN
        value: https://conc-slab-tributary-area-public.vercel.app
    plan: free
